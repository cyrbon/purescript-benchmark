{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "dependencies": {
    "benchmark": "^2.1.4"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript-psa": "^0.7.0",
    "rimraf": "^2.5.0"
  }
}
